import items from "./items.ts";
import Character from "./Character.ts";
export default class useItems extends Character {

    useItems (targets : Character[]){ 
        /**
         * fonction pour faire que la fonction verifie l'utilisation de l'objet 
         */




        
    }
}
